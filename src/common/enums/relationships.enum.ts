export enum Relationships {
  PENDING = 'pending',
  ACCEPTED = 'accepted',
  CANCELLED = 'cancelled',
  REJECTED = 'rejected',
  FRIENDS = 'friends',
  BLOCKED_BY_ACTOR = 'blocked_by_actor',
  BLOCKED_BY_TARGET = 'blocked_by_target',
  BLOCKED_BY_BOTH = 'blocked_by_both',
}
